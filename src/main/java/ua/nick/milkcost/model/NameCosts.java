package ua.nick.milkcost.model;

public enum NameCosts {

    GAS(), 
    REPAIRS(), 
    MATERIALS(), 
    OTHER(), 
    FOOD(), 
    ELECTRICITY(), 
    SALARY(), 
    PAYROLL_TAX(), 
    /*RENT(), */
    AMORTIZATION(), 
    SERVICES(),

    ADMIN();

    NameCosts() {
    }
}
